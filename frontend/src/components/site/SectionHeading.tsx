// Standalone export of the section-heading primitive. Implementation lives in
// SiteShell.tsx so the existing pages keep working unchanged.
export { SectionHeader as SectionHeading } from "./SiteShell";
